This repository provides an example implementation of the Stock Ownership Intel API hosted on RapidAPI: https://rapidapi.com/mikekipnis/api/stock-ownership-intel

To build:
```
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```
